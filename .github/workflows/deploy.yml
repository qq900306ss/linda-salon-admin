name: Deploy to S3 + CloudFront

on:
  push:
    branches: [main]
  workflow_dispatch:

env:
  NEXT_PUBLIC_API_URL: https://hto562wdv4bdtiino5cksgesgy0qskpg.lambda-url.ap-northeast-1.on.aws
  # TODO: 改成你 admin 前端的 S3 bucket 名稱(S3 Console 可查)
  S3_BUCKET: linda-salon-admin-site-1
  # TODO: 改成 admin 的 CloudFront 發行版 ID(CloudFront Console 列表的 ID 欄,E 開頭)
  CLOUDFRONT_DISTRIBUTION_ID: 請填入admin的發行版ID

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Build static site
        run: npm run build

      - name: Configure AWS credentials
        uses: aws-actions/configure-aws-credentials@v4
        with:
          aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws-region: ap-northeast-1

      - name: Sync to S3
        run: aws s3 sync out/ "s3://$S3_BUCKET" --delete

      - name: Invalidate CloudFront cache
        run: aws cloudfront create-invalidation --distribution-id "$CLOUDFRONT_DISTRIBUTION_ID" --paths "/*"
